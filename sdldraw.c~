#include <stdlib.h>                                                             
#include <stdio.h>                                                              
#include <unistd.h>                                                             
#include <sys/types.h>                                                          
#include <sys/stat.h>                                                           
#include <fcntl.h>                                                              
                                                                                
#include <SDL2/SDL.h>                                                           
#define SDL_MAIN_HANDLED                                                        
                                                                                
unsigned int framenumber = 0;

int main(int argc, char** argv) {

  unsigned int* image_frame;

  unsigned int width, height;

  
  SDL_Window *image_window = NULL;
  SDL_Window *tools_window = NULL;
  SDL_Window *colors_window = NULL;
  SDL_Renderer *image_renderer = NULL;
  SDL_Renderer *tools_renderer = NULL;
  SDL_Renderer *colors_renderer = NULL;

  
  SDL_Event event;
  SDL_Texture *texture;
  unsigned int pitch;

  SDL_SetMainReady();

  
  
  SDL_Init(SDL_INIT_VIDEO | SDL_INIT_EVENTS);

  image_window = SDL_CreateWindow("Image",100, 100, 640, 480, 0);
  tools_window = SDL_CreateWindow("Tools",10,100, 80, 480);
  colors_window = SDL_CreateWindow("colors",100, 490, 100, 10);

  #ifdef SOFTWARE_RENDERING
  renderer = SDL_CreateRenderer(win,-1,SDL_RENDERER_SOFTWARE);
#else
  renderer = SDL_CreateRenderer(win,-1,SDL_RENDERER_ACCELERATED);
#endif

  while(1) {
    
  }
}
